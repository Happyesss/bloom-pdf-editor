/**
 * Image Engine — Phase 4
 * Re-exports decoder; future home for JPEG2000, JBIG2, CCITT.
 */

export { decodeImage } from '../render/image-decoder';
export type { DecodedImage } from '../render/image-decoder';
